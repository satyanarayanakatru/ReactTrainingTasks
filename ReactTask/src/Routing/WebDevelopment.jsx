import React from "react";

function WebDevelopment() {
  return (
    <div className="nestingContainer">
      <h1>Nested Routing</h1>
      <h2>Web Application Developments.</h2>
      <span>Web Application Developments</span>
      <p>
        They are built using web technologies like HTML, CSS, and JavaScript but
        run inside a native app shell. This shell allows them to access device
        features such as the camera, GPS, or push notifications. Developers can
        deploy hybrid apps across multiple platforms like iOS and Android,
        saving time and cost compared to building separate native apps.
      </p>
    </div>
  );
}

export default WebDevelopment;
